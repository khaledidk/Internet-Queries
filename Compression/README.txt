Students :
Name: Khaled Idkedik ID: 207395153
Name: Salah Aldin Jawabreh ID: 086958006

Internet queries course Students

1st Exercise : The structure of the index

this ZIP file contains :
--IndexWriter.py :exercise requirment code for writing from reviews storage to seperated text files (products.lst/dic, text.lst/dic, review.tbl/ collection.data)
--IndexReader.py :exercise requirment code for reading from created dictionaries from IndexWriter.py class 
-- CompressedIndexWriter.py : exercise  2.
-- CompressedIndexReader.py : exercise 2.
--readme file :which is this file


extra function we used:

CompressedIndexWriter.py

1- def startFile(self, Out_Dir, In_Dir, file, data): This function write to all Compressed.

2- def divideIntToShortSpecific(self, number, short): This function convert the number to list and number of bytes depend what number they recive.

3- def howManyBytes(self, number): THis function count how many byytes that number need.
.
4- def unlpv(self, array): This function do decoded to length precoded variant, recive a list of number.

5- def unlpcfornumber(self, string): This function do decoded by length precoded variant, recive a number.

6- def numToLvp(self, number): This function do encoded by length precoded variant, recive a number.

7- def calculateGapsForlvp(self, Array): This function convert a array of review id and frequency to gapping.

8- def calculateGaps(self, array): This function convert a array frequency to gapping.

9- def variantValueToNumber(self, string): This function do encoded by variant, recive a String.

10- def divideIntToShort(self, number): This function convert the number to list of 4 bytes.

11- def variantCodeForNumber(self, number):  This function do encoded by variant, recive a number.

12 - def variantCode(self, numbersArray): This function do encoded by variant, recive a numbersArray.

13 - def howManyBytesInStrBinary(self, number): This count how many bytes for string of binary.